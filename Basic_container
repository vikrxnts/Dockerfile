
## Basic Of Container

what is container 

Container is a isolated process OR a Sandboxed Environment that running a Application on you machine

- Basic Manner Container is like a Mobile app

when you download or install a Mobile app ( A ) only app A related pkgs are install and app A is not affect to App B so means they run in isolated environment

Imp of Container : 

#  Self-contained ->  Each container has everything it needs to function with no reliance on any pre-installed dependencies on the host machine.
#  Isolated       ->  Since containers run in isolation, they have minimal influence on the host and other containers, increasing the security of your applications.
#  Independent    ->  Each container is independently managed. Deleting one container won't affect any others.
#  Portable       ->  Containers can run anywhere! The container that runs on your development machine will work the same way in a data center or anywhere in the cloud!


## Container Image

A container image is a standardized package that includes all of the files, binaries, libraries, and configurations to run a container

because of the docker image its application run every where because of container images have all things it it. 

</>

Seeing as a container is an isolated process, where does it get its files and configuration? How do you share those environments?

That's where container images come in. A container image is a standardized package that includes all of the files, binaries, libraries, and configurations to run a container.


principles of images 

  1) Images are immutable. Once an image is created, it can't be modified. You can only make a new image or add changes on top of it.
  2) Container images are composed of layers. Each layer represents a set of file system changes that add, remove, or modify files

## Docker Hub

Docker Hub is the default global marketplace for storing and distributing images. It has over 100,000 images created by developers that you can run locally


## List the image's layers using the docker image history command

$ docker image history < image_name >



## Registry

Registry is basically you can push image too registry and pull images from registry 

- Registry contain repository you can stored the container images

In registry you can create repository using - 1) DockerHUB
                                              2) Amazon ECR
                                              3) Github Container Registry
                                              4) Azure Container Registry
                                              5) Interal Registry for your organization

# Registry  VS   Repository


A registry is a centralized location that stores and manages container images

A repository is a collection of related container images within a registry

















